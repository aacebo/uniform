import { UniCardAvatarDirective } from './card-avatar.directive';

describe('UniCardAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new UniCardAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
