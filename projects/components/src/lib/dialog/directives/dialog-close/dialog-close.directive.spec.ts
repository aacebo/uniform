import { UniDialogCloseDirective } from './dialog-close.directive';

describe('UniDialogCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new UniDialogCloseDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
