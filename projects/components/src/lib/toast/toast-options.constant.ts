import { InjectionToken } from '@angular/core';

import { IUniToastOptions } from './toast-options.interface';

export const UNI_TOAST_OPTIONS = new InjectionToken<IUniToastOptions>('UNI_TOAST_OPTIONS');
